import { StyledModal } from "./style";

interface ICardModal {
    id: string;
}

export const CardModal = ({ id }: ICardModal) => {
    return (
        <>
            <StyledModal>
                asdfasdf
            </StyledModal>
        </>
    )
}